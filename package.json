{
  "name": "userscript-javstore",
  "private": true,
  "description": "JavStore Full Layout Cleanup userscript",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "jsdom": "^27.0.0"
  }
}
